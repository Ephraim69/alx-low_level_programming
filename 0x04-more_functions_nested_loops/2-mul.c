#include "main.h"

/**
* mul - multiplies two integers
* @a: first integer to be multiplied
* @b: second integer to be multiplied
* Return: returns the multiplied value in a variable m
*/
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);

}
